export { createStore } from "./createStore";

// Example
// const store = createStore((set) => ({
//   counter: 0,
//   addCount: (count: number) =>
//     set((state) => {
//       state.counter += count;
//     }),
// }));
